# Fluss + Flink Cluster Configuration
# Set HOST_IP to your server's external IP address for remote access.
# OpenClaw on other machines will connect to this IP.
#
# Example:
#   HOST_IP=192.168.1.100
#
# If only testing locally, you can leave this as 0.0.0.0
HOST_IP=0.0.0.0
