{
  "id": "fluss-hook",
  "name": "Fluss Hook Event Logger",
  "description": "Log all OpenClaw hook events to Apache Fluss for real-time analytics",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "gatewayUrl": {
        "type": "string",
        "description": "Fluss Gateway REST API URL (e.g. http://localhost:8080)"
      },
      "gatewayUsername": {
        "type": "string",
        "description": "Fluss Gateway basic auth username (optional)"
      },
      "gatewayPassword": {
        "type": "string",
        "description": "Fluss Gateway basic auth password (optional)"
      },
      "databaseName": {
        "type": "string",
        "description": "Fluss database name"
      },
      "tablePrefix": {
        "type": "string",
        "description": "Table name prefix for hook event tables (e.g. hook_ creates hook_agent_end, hook_session_start, etc.)"
      },
      "batchSize": {
        "type": "integer",
        "description": "Max buffered rows per table before flush"
      },
      "flushIntervalMs": {
        "type": "integer",
        "description": "Flush interval in milliseconds"
      },
      "autoCreateTable": {
        "type": "boolean",
        "description": "Auto-create Fluss database and tables if not exists"
      },
      "bucketCount": {
        "type": "integer",
        "description": "Number of buckets for table distribution"
      },
      "outputMode": {
        "type": "string",
        "enum": ["fluss", "memory", "console"],
        "description": "Output mode: fluss (production), memory (test), console (debug)"
      }
    }
  }
}
